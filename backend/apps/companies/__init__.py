"""Tenant / company module placeholder.

The Company model, onboarding, and tenant isolation are implemented in a
later prompt. This app exists so URLs and settings can include it now
without locking in a premature schema.
"""
