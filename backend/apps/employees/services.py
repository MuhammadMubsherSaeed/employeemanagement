"""Employee writes stay in serializers + tenant mixins.

This module is the place to hook future audit events
(employee created, manager changed, status changed).
"""


class EmployeeService:
    """Reserved for trusted server-side employee operations."""
